import {createFileRoute} from '@tanstack/react-router';import {useRoadData} from '@/lib/roadsense-data';import {PageTitle,Status} from '@/components/roadsense/shared';import {TrendChart} from '@/components/roadsense/charts';import {BarChart,Bar,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';
export const Route=createFileRoute('/trends')({head:()=>({meta:[{title:'Trends & Exposure — RoadSense Kerala'},{name:'description',content:'Weekly severity trends and passenger exposure rankings.'},{property:'og:title',content:'Trends & Exposure — RoadSense Kerala'},{property:'og:description',content:'Track changing segment indicators and fleet exposure.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const d=useRoadData();const ranked=[...d.summaries].sort((a,b)=>(b.exposure_score??0)-(a.exposure_score??0)).slice(0,8);const trend=Array.from({length:8},(_,i)=>({name:`W${i+1}`,value:2.3+Math.sin(i*.8)*.6+i*.09}));return <><PageTitle title="Trends & Exposure" subtitle="Monitor weekly change and prioritize frequently travelled segments."/><div className="two-col"><section className="panel chart-panel"><div className="card-heading"><h3>Weekly severity trend</h3><span>All monitored segments</span></div><div className="h-72"><TrendChart data={trend}/></div></section><section className="panel changes"><div className="card-heading"><h3>Recent changes</h3><span>Week over week</span></div>{ranked.slice(0,5).map((r,i)=><div key={r.segment_id}><span><b>{r.segment_id}</b><small>Severity {r.median_severity}</small></span><Status kind={i===1?'new':i===3?'repaired':'worsened'}/></div>)}</section></div><section className="panel chart-panel"><div className="card-heading"><div><h3>Passenger exposure ranking</h3><p>Passes × average passengers onboard × segment length</p></div></div><div className="h-80"><ResponsiveContainer width="100%" height="100%"><BarChart data={ranked} layout="vertical"><CartesianGrid horizontal={false} strokeDasharray="3 3"/><XAxis type="number"/><YAxis dataKey="segment_id" type="category" width={75} fontSize={11}/><Tooltip/><Bar dataKey="exposure_score" fill="var(--primary)" radius={[0,5,5,0]}/></BarChart></ResponsiveContainer></div></section></>}});
