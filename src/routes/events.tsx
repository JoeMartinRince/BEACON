import {createFileRoute,useNavigate} from '@tanstack/react-router';import {useRoadData} from '@/lib/roadsense-data';import {PageTitle} from '@/components/roadsense/shared';import {EventsTable} from '@/components/roadsense/data-table';import {Tabs,TabsContent,TabsList,TabsTrigger} from '@/components/ui/tabs';
export const Route=createFileRoute('/events')({head:()=>({meta:[{title:'Events — RoadSense Kerala'},{name:'description',content:'Detected road events and suppressed manoeuvres.'},{property:'og:title',content:'Road Events — RoadSense Kerala'},{property:'og:description',content:'Review detected events and motion-context suppression.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const d=useRoadData();const nav=useNavigate();return <><PageTitle title="Events" subtitle="Review detections and motion-context suppression."/><Tabs defaultValue="events"><TabsList><TabsTrigger value="events">Detected events ({d.events.length})</TabsTrigger><TabsTrigger value="suppressed">Suppressed manoeuvres ({d.suppressed.length})</TabsTrigger></TabsList><TabsContent value="events"><EventsTable rows={d.events} onLocate={id=>nav({to:'/road-map',search:{segment:id}})}/></TabsContent><TabsContent value="suppressed"><div className="mb-3 note">These records were classified as driver motion and excluded from road-condition reporting.</div><EventsTable rows={d.suppressed}/></TabsContent></Tabs></>}});
