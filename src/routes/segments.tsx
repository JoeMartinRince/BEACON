import {createFileRoute} from '@tanstack/react-router';import {useRoadData} from '@/lib/roadsense-data';import {PageTitle} from '@/components/roadsense/shared';import {SegmentsTable} from '@/components/roadsense/data-table';
export const Route=createFileRoute('/segments')({head:()=>({meta:[{title:'Segments — Beacon'},{name:'description',content:'Ranked corridor segment condition indicators.'},{property:'og:title',content:'Road Segments — Beacon'},{property:'og:description',content:'Searchable segment rankings by severity, confidence and exposure.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=>{const d=useRoadData();return <><PageTitle title="Segments" subtitle="Ranked by median severity and passenger exposure."/><SegmentsTable rows={d.summaries}/></>}});
