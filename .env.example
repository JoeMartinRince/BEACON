# ==============================================================================
# BEACON - Environment Configuration Template
# Placeholders only - do not commit real secrets to source control.
# ==============================================================================

# Client & Server (Public - governed by Supabase RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server / CLI Only (Dataset Ingestion & Condition Aggregation Scripts)
# WARNING: Never expose this key in frontend code or commit to source control.
SUPABASE_SERVICE_ROLE_KEY=
